version: 0.2

phases:
  pre_build:
    commands:
       - terraform --version
       - terraform init
       - terraform validate
  build:
    commands:
       - terraform plan
  post_build:
    commands:
       - echo terraform plan completed on `date`